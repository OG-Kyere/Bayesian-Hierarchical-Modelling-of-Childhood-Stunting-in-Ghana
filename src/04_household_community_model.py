"""Three-level Bayesian logistic model with household and community effects.

Planned form:
    Y_ijk ~ Bernoulli(p_ijk)
    logit(p_ijk) = alpha + X_ijk beta + u_k + v_jk
"""

def main():
    raise NotImplementedError("Requires prepared row-level household and community identifiers.")

if __name__ == "__main__":
    main()
