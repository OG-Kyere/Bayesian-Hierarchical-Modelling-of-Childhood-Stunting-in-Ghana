# Pilot environment lock

This file records the exact software versions stored in the completed community-level pilot model metadata. It is retained for reproducibility of Model 0 only.

pymc==6.3.2
arviz==1.3.0
numpy==2.3.5
pandas==2.2.3

# This file applies only to the historical pilot. The current reproducibility
# environment and compatibility notes are documented in requirements.txt and
# docs/executed_environment_2026-09-23.md.
